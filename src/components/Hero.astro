---
import { getLatestRelease } from '../lib/github';

const release = await getLatestRelease();
---

<section class="hero" id="hero">
  <div class="hero__bg">
    <div class="hero__glow hero__glow--1"></div>
    <div class="hero__glow hero__glow--2"></div>
  </div>
  
  <div class="container hero__container">
    <div class="hero__content">
      <div class="hero__badge animate-fade-in-up">
        <span class="hero__badge-dot"></span>
        <span>Versión {release.version} disponible</span>
        <span class="hero__badge-tag">Open Source</span>
      </div>
      
      <h1 class="hero__title animate-fade-in-up delay-100">
        Revoluciona tu experiencia con 
        <span class="text-gradient">mouse y trackpad</span>
        en macOS
      </h1>
      
      <p class="hero__description animate-fade-in-up delay-200">
        BuenMouse es la herramienta de productividad definitiva para macOS. 
        Activa Mission Control, navega entre Spaces, y personaliza cada aspecto 
        de tu mouse con latencia casi imperceptible.
      </p>
      
      <div class="hero__actions animate-fade-in-up delay-300">
        <a href="https://github.com/StevenACZ/BuenMouse/releases" class="btn btn--primary btn--lg" target="_blank" rel="noopener noreferrer">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4M7 10l5 5 5-5M12 15V3"/>
          </svg>
          Descargar Gratis
        </a>
        <a href="https://github.com/StevenACZ/BuenMouse" class="btn btn--secondary btn--lg" target="_blank" rel="noopener noreferrer">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0112 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/>
          </svg>
          Ver en GitHub
        </a>
      </div>
      
      <div class="hero__stats animate-fade-in-up delay-400">
        <div class="hero__stat">
          <span class="hero__stat-value">&lt;1ms</span>
          <span class="hero__stat-label">Latencia</span>
        </div>
        <div class="hero__stat-divider"></div>
        <div class="hero__stat">
          <span class="hero__stat-value">~15MB</span>
          <span class="hero__stat-label">Memoria</span>
        </div>
        <div class="hero__stat-divider"></div>
        <div class="hero__stat">
          <span class="hero__stat-value">MIT</span>
          <span class="hero__stat-label">Licencia</span>
        </div>
      </div>
    </div>
    
    <div class="hero__visual animate-fade-in-up delay-300">
      <div class="hero__mockup">
        <div class="hero__mockup-window">
          <div class="hero__mockup-header">
            <div class="hero__mockup-dots">
              <span class="hero__mockup-dot hero__mockup-dot--red"></span>
              <span class="hero__mockup-dot hero__mockup-dot--yellow"></span>
              <span class="hero__mockup-dot hero__mockup-dot--green"></span>
            </div>
            <div class="hero__mockup-title-wrapper">
              <img src="/icon-64.png" alt="" width="24" height="24" class="hero__mockup-icon" />
              <span class="hero__mockup-title">BuenMouse</span>
            </div>
          </div>
          <div class="hero__mockup-content">
            <div class="hero__mockup-toggle"><span>Mission Control</span><div class="hero__mockup-switch hero__mockup-switch--active"></div></div>
            <div class="hero__mockup-toggle"><span>Navegación Spaces</span><div class="hero__mockup-switch hero__mockup-switch--active"></div></div>
            <div class="hero__mockup-toggle"><span>Zoom Dinámico</span><div class="hero__mockup-switch hero__mockup-switch--active"></div></div>
            <div class="hero__mockup-toggle"><span>Scroll Invertido</span><div class="hero__mockup-switch"></div></div>
            <div class="hero__mockup-toggle"><span>Inicio Automático</span><div class="hero__mockup-switch hero__mockup-switch--active"></div></div>
            <div class="hero__mockup-footer">
              <div class="hero__mockup-status"><span class="hero__mockup-status-dot"></span>Activo</div>
              <span class="hero__mockup-version">v{release.version}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
.hero { position: relative; min-height: 100vh; display: flex; align-items: center; padding-top: calc(var(--space-24) + 73px); padding-bottom: var(--space-16); overflow: hidden; }
.hero__bg { position: absolute; inset: 0; pointer-events: none; }
.hero__glow { position: absolute; border-radius: 50%; filter: blur(100px); opacity: 0.5; }
.hero__glow--1 { top: -20%; left: -10%; width: 60%; height: 60%; background: radial-gradient(circle, rgba(34, 211, 238, 0.4) 0%, transparent 70%); }
.hero__glow--2 { bottom: -30%; right: -10%; width: 50%; height: 50%; background: radial-gradient(circle, rgba(6, 182, 212, 0.3) 0%, transparent 70%); }
.hero__container { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-16); align-items: center; }
.hero__content { max-width: 600px; }
.hero__badge { display: inline-flex; align-items: center; gap: var(--space-2); padding: var(--space-2) var(--space-4); background: var(--color-bg-glass); border: 1px solid rgba(255, 255, 255, 0.08); border-radius: var(--radius-full); font-size: var(--text-sm); color: var(--color-text-secondary); margin-bottom: var(--space-6); }
.hero__badge-dot { width: 8px; height: 8px; background: var(--color-success); border-radius: 50%; animation: pulse 2s ease-in-out infinite; }
.hero__badge-tag { padding: var(--space-1) var(--space-2); background: var(--gradient-primary); border-radius: var(--radius-sm); font-size: var(--text-xs); font-weight: 600; color: white; }
.hero__title { margin-bottom: var(--space-6); line-height: 1.1; }
.hero__description { font-size: var(--text-lg); margin-bottom: var(--space-8); max-width: 540px; }
.hero__actions { display: flex; flex-wrap: wrap; gap: var(--space-4); margin-bottom: var(--space-10); }
.hero__stats { display: flex; align-items: center; gap: var(--space-6); }
.hero__stat { display: flex; flex-direction: column; gap: var(--space-1); }
.hero__stat-value { font-size: var(--text-xl); font-weight: 700; color: var(--color-text-primary); }
.hero__stat-label { font-size: var(--text-sm); color: var(--color-text-muted); }
.hero__stat-divider { width: 1px; height: 40px; background: rgba(255, 255, 255, 0.1); }
.hero__visual { display: flex; justify-content: center; perspective: 1000px; }
.hero__mockup { transform: rotateY(-5deg) rotateX(5deg); transition: transform var(--transition-slow); }
.hero__mockup:hover { transform: rotateY(0) rotateX(0); }
.hero__mockup-window { width: 380px; background: rgba(30, 30, 40, 0.9); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: var(--radius-xl); overflow: hidden; box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5); backdrop-filter: blur(20px); }
.hero__mockup-header { display: flex; align-items: center; padding: var(--space-4); background: rgba(255, 255, 255, 0.02); border-bottom: 1px solid rgba(255, 255, 255, 0.05); }
.hero__mockup-dots { display: flex; gap: var(--space-2); }
.hero__mockup-dot { width: 12px; height: 12px; border-radius: 50%; }
.hero__mockup-dot--red { background: #ff5f57; }
.hero__mockup-dot--yellow { background: #febc2e; }
.hero__mockup-dot--green { background: #28c840; }
.hero__mockup-title-wrapper { display: flex; align-items: center; justify-content: center; gap: var(--space-2); flex: 1; }
.hero__mockup-icon { border-radius: 4px; }
.hero__mockup-title { font-size: var(--text-sm); font-weight: 500; color: var(--color-text-secondary); }
.hero__mockup-content { padding: var(--space-4); display: flex; flex-direction: column; gap: var(--space-3); }
.hero__mockup-toggle { display: flex; align-items: center; justify-content: space-between; padding: var(--space-3) var(--space-4); background: rgba(255, 255, 255, 0.03); border-radius: var(--radius-lg); font-size: var(--text-sm); color: var(--color-text-secondary); }
.hero__mockup-switch { width: 44px; height: 24px; background: rgba(255, 255, 255, 0.1); border-radius: var(--radius-full); position: relative; }
.hero__mockup-switch::after { content: ''; position: absolute; top: 2px; left: 2px; width: 20px; height: 20px; background: white; border-radius: 50%; transition: all var(--transition-fast); }
.hero__mockup-switch--active { background: var(--gradient-primary); }
.hero__mockup-switch--active::after { left: calc(100% - 22px); }
.hero__mockup-footer { display: flex; align-items: center; justify-content: space-between; padding-top: var(--space-4); border-top: 1px solid rgba(255, 255, 255, 0.05); }
.hero__mockup-status { display: flex; align-items: center; gap: var(--space-2); font-size: var(--text-sm); color: var(--color-success); }
.hero__mockup-status-dot { width: 8px; height: 8px; background: var(--color-success); border-radius: 50%; animation: pulse 2s ease-in-out infinite; }
.hero__mockup-version { font-size: var(--text-sm); color: var(--color-text-muted); }
@media (max-width: 1024px) { .hero__container { grid-template-columns: 1fr; text-align: center; } .hero__content { max-width: 100%; } .hero__actions, .hero__stats { justify-content: center; } .hero__visual { order: -1; } .hero__mockup { transform: none; } .hero__mockup-window { width: 100%; max-width: 380px; } }
@media (max-width: 480px) { .hero { padding-top: calc(var(--space-16) + 73px); } .hero__actions { flex-direction: column; } .hero__actions .btn { width: 100%; justify-content: center; } .hero__stats { flex-direction: column; gap: var(--space-4); } .hero__stat-divider { width: 40px; height: 1px; } }
</style>
